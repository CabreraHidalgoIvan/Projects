<form method="post" action="{{ path('app_estado_tarea_delete', {'id': estado_tarea.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ estado_tarea.id) }}">
    <button class="btn btn-delete">Delete</button>
</form>
